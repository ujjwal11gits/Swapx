/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'encrypted-tbn0.gstatic.com',
            'localhost',
            'i.imgur.com'
        ],
    },
};

export default nextConfig;
